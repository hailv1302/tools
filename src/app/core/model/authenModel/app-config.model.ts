export interface AppConfig {
    APIUrl: string;
    authority: string;
    client_id: string;
    client_secret: string;
    redirect_uri: string;
    post_logout_redirect_uri: string;
    response_type: string;
    scope: string;
    portal_url: string;
    BACKEND_URL: string;
}
